using UnityEngine;

public class test_item_class_GT : MonoBehaviour
{

    /*
    Item_class2 rock, sword, potion, hat;
    consumable_consume_test potion, drink;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        rock = new Item_class2("Rock", 0, "This is one boring ahh rock", 1.2f);
        sword = new Item_class2("Sword", 1, "Swow", 0.9f);
        potion = new consumable_consume_test("Potion", 2, "pot", 0.2f, -50);
        hat = new Item_class2("Cap", 3, "AHA-t", 0.1f);
    }

    public 
    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.R)) { potion.DisplayInfo(); }
    }
}
    */
}